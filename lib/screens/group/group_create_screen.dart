import 'package:flutter/material.dart';

class GroupCreateScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('그룹 생성')),
      body: Center(child: Text('그룹을 생성하는 화면')),
    );
  }
}